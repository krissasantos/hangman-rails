
<% blanks = "___ " * @game.word.length %>

    <h1><%= blanks %></h1>
        
        <% @alphabet.each_with_index do |letter, idx| %>
          

      <%= link_to letter, class:"btn btn-default", method: :patch, remote: true, params: { selected_letter: letter} %>
        <% end %>
                   



<div>
  <h2>incorrect guesses:</h2>
  <% @incorrect_guesses.join(",")%>
</div>
<br>


remove this later:
<h2><%= @game.word %></h2>